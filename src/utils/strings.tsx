const strings: any = {
  yesterday: 'Yesterday',
  today: 'Today',
  close_card: 'Close Card',
  dont_show_again: "Don't show again",
};

const stg = (key: string) => strings[key];

export default stg;
